import { create } from '@storybook/theming/create';

export default create({
  base: 'light',
  brandTarget: '_self',
  brandTitle: "Planta's UI Components",
  fontBase:
    "Inter, system-ui, -apple-system, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif",
  fontCode: 'monospace',
});
